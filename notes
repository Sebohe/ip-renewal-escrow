Each account should per user to each subnet dao

Forum post as comparison between the two subscription methods

the payout should go out of the system to the subnet dao.

Subnetdaos advertises, users request to join.
